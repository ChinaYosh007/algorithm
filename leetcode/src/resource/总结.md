2462题代码真的太妙了
~~~java
Comparator<Pair> comparator = this::compare; //比较器--> 抽象出来，后续继续用
...
private int compare(Pair a, Pair b) {
    if (a.value1 != b.value1) {
        return Integer.compare(a.value1, b.value1);
    }
        return Integer.compare(a.value2, b.value2);
}
/**
抽象比较方法，是否选左边堆
*/
 private boolean shouldChooseLeft(PriorityQueue<Pair> left, PriorityQueue<Pair> right) {
        if (right.isEmpty()) {
            return true;
        }

        if (left.isEmpty()) {
            return false;
        }

        return compare(left.peek(), right.peek()) <= 0;
    }
~~~
338这道题也是非常的哇塞，眼前一亮，利用算过的方式避免重复计算，简直是史家之绝唱，无韵之离骚
~~~java
   public int[] countBits(int n) {
    int[] popCount = new int[n + 1];
    popCount[0] = 0;
    for(int i = 1 ; i <= n ; i++){
        popCount[i] = popCount[i >> 1] + (i & 1);
    }
    return  popCount;
}
~~~
